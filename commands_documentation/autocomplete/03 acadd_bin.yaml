- id:      ACADD_BIN
  name:    ACADD_BIN
  usage:   ACADD_BIN key text exp
  params:
    items:
    - k: key
      v: name of autocomplete object
    - k: text
      v: text, must be ASCII or old style 8 bit encodings like CP1250, CP1251, KOI8 etc.
    - k: exp
      v: expiration in seconds
  description: |
    Add *text* into the autocomplete object *key*, with expiration of *exp* seconds.

  return_type: int
  return:      new score of the text
  version:     1.3.14.2
  complexity:  READ + 2 * (N + 1) * WRITE
  mutable:     true
  compatible:  false
  example:     |
    127.0.0.1:6379> ACADD_BIN mykey "hello world" 3600
    (integer) 1

  sql:
  see_also:
    items:
      - ACADD_UTF8
