- id:       ACDEL_BIN
  name:     ACDEL_BIN
  aliases:
    items:
    - ACREM_BIN
    - ACREMOVE_BIN
  usage:    ACDEL_BIN key text
  params:
    items:
    - k: key
      v: name of autocomplete object
    - k: text
      v: text, must be ASCII or old style 8 bit encodings like CP1250, CP1251, KOI8 etc.

  description: |
    Remove *text* from the autocomplete object *key*.

  return_type: bool
  return:      always returns 1
  version:     1.3.14.2
  complexity:  READ + (N + 1) * WRITE
  mutable:     true
  compatible:  false
  example:     |
    127.0.0.1:6379> ACDEL_BIN mykey "hello world"
    (integer) 1

  see_also:
    items:
    - ACDEL_UTF8
