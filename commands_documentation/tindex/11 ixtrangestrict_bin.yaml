- id:          IXTRANGESTRICT_BIN
  name:        IXTRANGESTRICT_BIN
  usage:       IXTRANGESTRICT_BIN key delimiter "words,words" count start
  params:
    items:
    - k: key
      v: name of the tindex object
    - k: delimiter
      v: delimiter, for example "," (coma)
    - k: words,words
      v: words, must be ASCII or old style 8 bit encodings like CP1250, CP1251, KOI8 etc.
    - k: count
      v: result count
    - k: start
      v: specify starting key (for pagination in the similar way as in XNGET)

  description: |
    Returns a range tindex object *key* using words *words,words*<br />
    The results apears, if contains ALL of the words.

  return_type: array
  return:      list of key / values with "next" key at the end.
  version:     1.3.14.2
  complexity:  "[number of words] * READ"
  mutable:     false
  compatible:  false
  example:     |
    127.0.0.1:6379> IXTADD_BIN mykey John , "london,new york" x "Data for John"
    (integer) 1
    127.0.0.1:6379> IXTADD_BIN mykey Pete , "london,boston" x "Data for Pete"
    (integer) 1
    127.0.0.1:6379> IXTADD_BIN mykey Leon , "london,new york,serdica" x "Data for Leon"
    (integer) 1
    127.0.0.1:6379> IXTRANGESTRICT_BIN mykey , 'london,new york' 100 ''
    1) "John"
    2) "Data for John"
    3) "Leon"
    4) "Data for Leon"
    5) ""

  see_also:
    items:
      - IXTRANGEFLEX_UTF8
      - IXTRANGEFLEX_BIN
      - IXTRANGESTRICT_UTF8

