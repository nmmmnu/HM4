- id:      TYPE
  name:    TYPE
  usage:   TYPE key
  description: |
    Provided for compatibility.<br />
    Does not do anything.<br />

  return_type: string
  return:      always returns text "string"
  version:     1.2.16
  mutable:     n/a
  compatible:  true
